# SwiftProtobuf runtime provenance

- Distribution repository: <https://github.com/apple/swift-protobuf>
- Swift package tag: `1.32.0`
- Immutable package revision: `c6fe6442e6a64250495669325044052e113e990c`
- License: Apache-2.0 with the runtime-library exception. The upstream
  `LICENSE.txt` text is included as `SwiftProtobuf-LICENSE.txt` in this
  directory, with trailing whitespace normalized. Upstream does not publish a
  separate `NOTICE` file at this revision.
- Use: linked by the macOS host through the local Protocol v1 Swift package.
- Code copying: runtime source is resolved through Swift Package Manager; no
  SwiftProtobuf source is copied into Vibe Screen.

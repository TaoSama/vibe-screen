Current-base source setup:

  git fetch origin main --prune
  git rev-parse origin/main
  # e647b6dcd0ea18907d6812a4d5f692f9eb63dfcd

  git worktree add --detach <redacted-source-worktree> origin/main
  git -C <redacted-source-worktree> status --short --branch
  # ## HEAD (no branch)

Safety checks around the run:

  pgrep -x sfltool || true
  # empty; retained as sfltool-precheck.txt

Default Host readiness collection, with no login-item diagnostic opt-in flags:

  python3 scripts/macos_dev_host.py readiness \
    --source-root <redacted-source-worktree> \
    --report docs/changes/2026-08-14-phase-2-tablet-productization/evidence/2026-08-29-macos-login-headless-current-base-blocked/host-signing-and-permissions.txt \
    --json-output docs/changes/2026-08-14-phase-2-tablet-productization/evidence/2026-08-29-macos-login-headless-current-base-blocked/host-readiness.json \
    --port 54321
  # exit 2, expected blocked readiness; retained as host-readiness.exit

The command intentionally did not pass any of these flags:

  --include-login-item-diagnostic
  --inspect-login-items
  --probe-login-item
  --probe-login-items

The generated host-readiness.json records:

  status=blocked
  login_headless_status=blocked
  can_start_headless_login_gate=false
  login_headless.login_item.state=unverified
  login_headless.login_item.sfltool_dumpbtm_was_run=false

Passive evidence input generation:

  python3 - <<'PY'
  # Read host-readiness.json and write macos-startup-recovery-evidence.json
  # with all real reboot/login, headless capture, client render, bounded
  # recovery, window restore, and Android reconnect observations set false.
  PY

Passive startup/recovery gate derivation:

  PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tools python3 -m vibescreen_evidence.macos_startup_recovery_gate \
    --evidence docs/changes/2026-08-14-phase-2-tablet-productization/evidence/2026-08-29-macos-login-headless-current-base-blocked/macos-startup-recovery-evidence.json \
    --output docs/changes/2026-08-14-phase-2-tablet-productization/evidence/2026-08-29-macos-login-headless-current-base-blocked/macos-startup-recovery-gate.json
  # exit 1, expected blocked gate; retained as macos-startup-recovery-gate.exit

Phase 2 aggregate owner check:

  make phase2-aggregate-owner \
    EVIDENCE_DIR=docs/changes/2026-08-14-phase-2-tablet-productization/evidence/2026-08-29-macos-login-headless-current-base-blocked \
    PHASE2_LOGIN_HEADLESS=docs/changes/2026-08-14-phase-2-tablet-productization/evidence/2026-08-29-macos-login-headless-current-base-blocked/macos-startup-recovery-gate.json
  # exit 0; aggregate remains verdict=blocked and can_close_readme_phase2_gates=false

Final safety check:

  pgrep -x sfltool || true
  # empty; retained as sfltool-postcheck.txt

Focused verification after the code/tooling update:

  PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile scripts/macos_dev_host.py scripts/tests/test_macos_dev_host.py
  PYTHONDONTWRITEBYTECODE=1 python3 -m unittest scripts.tests.test_macos_dev_host -v
  PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tools python3 -m unittest tools.tests.test_macos_startup_recovery_gate tools.tests.test_phase2_aggregate_owner tools.tests.test_schemas -v
  make baseline-macos-host-readiness EVIDENCE_DIR=.build/evidence/macos-login-headless-default-readiness-check
  # exit 2, expected blocked readiness. The generated JSON reports
  # login_headless.login_item.sfltool_dumpbtm_was_run=false.

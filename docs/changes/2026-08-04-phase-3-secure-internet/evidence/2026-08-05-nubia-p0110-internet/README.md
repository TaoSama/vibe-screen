# Android Internet acceptance — 2026-08-05

This immutable evidence set records one combined PASS from clean, reachable
source commit `597518f948075e396352bc353afcec01a30303f3` on
`Nubia P0110 / pacific / Android 16`.

The combined runner installed the controlled app and instrumentation APKs and
ran the real `MainActivity` Internet credential UI plus the production Android
M144/macOS M150 product-session adapters. Both direct and forced local coturn
routes passed Protocol v1, AES-256-GCM control/media, synthetic video
config/keyframe/delta delivery, and authenticated touch. The UI pass covered the
Internet tab and route toggle, signed pairing, strict signed lease import, local
revoke, re-pair, and secure credential dialogs through production View
listeners and AndroidKeyStore-backed stores.

`acceptance.json` binds both routes to the same source, device lease snapshot,
and controlled artifacts. Each route contains 24 ADB subprocesses and 48
append-only `adb-gates.jsonl` records: one valid `before` and `after` record per
subprocess. Gate records expose no lock owner value; they record owner/bytes
equality, live holder PID, task, commit, filesystem device/inode, byte length,
and the logical command name.

The archived files were scanned for the private ADB address, hardware identifier,
session/TURN credentials, tokens, URLs, IP literals, and local absolute paths.
The archived coturn logs are structure-preserving redacted derivatives: their
IP/path values are placeholders, while `acceptance.json` retains the SHA-256 of
the private original and `SHA256SUMS` binds the committed derivative. Earlier
failed attempts and private originals remain outside the repository and are not
part of this evidence set.

This run does **not** prove ScreenCaptureKit or other real display capture,
visible Mac input effects, Android device rotation, disconnect/reconnect or
network handoff, cross-service revocation, public NAT/TURN, packet-capture
confidentiality, latency, or soak. Media was generated by the synthetic
Protocol v1 host, and revocation/re-pair evidence is local to Android credential
and KeyStore lifecycle.

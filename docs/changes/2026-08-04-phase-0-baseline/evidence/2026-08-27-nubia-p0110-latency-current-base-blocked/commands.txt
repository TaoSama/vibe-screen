# Commands

Repository state:

```bash
git fetch origin --prune
git rev-parse HEAD
git rev-parse origin/main
git remote get-url origin
```

Result: `HEAD` and `origin/main` were both
`3b2ba11e832a3618eaedfc67f92414b161423a00`; origin is GitHub.

Device identity checks used the required explicit ADB target form. The raw
Android serial is redacted from this public record.

```bash
adb -s <redacted-serial> get-state
adb -s <redacted-serial> shell getprop ro.product.manufacturer
adb -s <redacted-serial> shell getprop ro.product.model
adb -s <redacted-serial> shell getprop ro.product.device
adb -s <redacted-serial> shell getprop ro.product.name
adb -s <redacted-serial> shell getprop ro.build.version.release
adb -s <redacted-serial> shell getprop ro.build.version.sdk
adb -s <redacted-serial> shell getprop ro.build.fingerprint
```

Observed values:

```text
state: device
manufacturer: nubia
model: P0110
device: pacific
product: pacific
Android release: 16
SDK: 36
fingerprint: nubia/pacific/pacific:[redacted-fingerprint-tail]
```

Device-info generation used:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tools \
  python3 -m vibescreen_evidence.device_info \
  --serial <redacted-serial> \
  --no-connect \
  --output docs/changes/2026-08-04-phase-0-baseline/evidence/2026-08-27-nubia-p0110-latency-current-base-blocked/device-info.raw.json
```

The generated file was then redacted so both `adb_serial` and `device_serial`
are `nubia-p0110-pacific-device-1`, and the local Android SDK path in
`adb_version` is replaced with `[redacted-android-sdk]`.

Camera and formal-package preflight used read-only checks:

```bash
system_profiler SPCameraDataType
find docs/changes/2026-08-04-phase-0-baseline/evidence -maxdepth 2 -name raw-camera.mov -print
find docs/changes/2026-08-04-phase-0-baseline/evidence -maxdepth 2 -name manifest.json -print
find docs/changes/2026-08-04-phase-0-baseline/evidence -maxdepth 2 -name samples.csv -print
find docs/changes/2026-08-04-phase-0-baseline/evidence -maxdepth 2 -name latency-evidence-report.json -print
find docs/changes/2026-08-04-phase-0-baseline/evidence -maxdepth 2 -name latency-preflight.json -print
```

Result: only the built-in camera was visible locally, and no retained formal
latency package artifacts were found for the three README profiles. Prior
`latency-preflight.json` files were found for the 2026-08-22 and 2026-08-24
blocked readiness records.

Latency preflight command:

```bash
make evidence-latency-preflight \
  EVIDENCE_DIR=docs/changes/2026-08-04-phase-0-baseline/evidence/2026-08-27-nubia-p0110-latency-current-base-blocked \
  LATENCY_DEVICE_INFO=docs/changes/2026-08-04-phase-0-baseline/evidence/2026-08-27-nubia-p0110-latency-current-base-blocked/device-info.json \
  LATENCY_PREFLIGHT_INPUT=docs/changes/2026-08-04-phase-0-baseline/evidence/2026-08-27-nubia-p0110-latency-current-base-blocked/preflight-input.json \
  LATENCY_REPOSITORY_REVISION=3b2ba11e832a3618eaedfc67f92414b161423a00
```

Result: exit `2`, meaning blocked readiness rather than malformed input or a
failed latency measurement. The generated `latency-preflight.json` reports
`status=blocked` and `can_close_performance_gate=false` for all three profiles.

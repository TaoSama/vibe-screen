# Commands

Repository state:

```bash
pgrep -x sfltool || true
git fetch origin main --prune
git switch -c codex/p0110-latency-current-base-20260828 origin/main
git rev-parse HEAD
git rev-parse origin/main
git remote get-url origin
```

Result: no `sfltool` process was present at the start of the run. The initial
branch was created from `origin/main` at
`20cd27b1d59dfcc66e28df41aba421e14b6171f4`. During CI babysitting, `origin/main`
advanced to `47361dd7f56fefef50da55a69e0368531c8ada33`, then to
`181167a81ea3176be466c5e5ec7c0c02bb915038`, then to
`3c68a48b4646590f5761962a41913f42690a8818`, then to
`b697ddae169bb40328a7c4cb8ba92231210c12bf`, then to
`6d8a89ae296b9adc9f357e618e3f93570632fba3`, then to
`703f96fae3536fed2216bcae6cae0a7e71bf0158`, then to
`567dae75da22b2faa49ab59e5d95b4a642be1d97`, then to
`dd555918f9313d40fa9b2b3dc45c8a12cc44c20c`, then to
`07bb40e18074fd737c4a70714b4faf4499ea64ab`, then to
`9e6621b7194bf5aa051a07944afb6e2b1ccf2232`, then to
`10f5df00067009b4e86991a617f2f29e61ad5eeb`, then to
`869ef6d0a05f19f707b100b8a3c5ff78c5a28021`, then to
`dd6978cb5e8e36b6aa15995361ed28ee54cd6b3e`; the branch was updated and this
evidence record was refreshed against the latest current-base commit. Origin is
GitHub. No `/usr/bin/sfltool dumpbtm` command was executed, and no login-item
diagnostic opt-in argument was passed.

Device identity checks used the required explicit ADB target form. Public
artifacts redact the raw Android serial.

```bash
adb -s <redacted-serial> get-state
adb -s <redacted-serial> shell getprop ro.product.manufacturer
adb -s <redacted-serial> shell getprop ro.product.model
adb -s <redacted-serial> shell getprop ro.product.device
adb -s <redacted-serial> shell getprop ro.build.version.release
adb -s <redacted-serial> shell getprop ro.build.version.sdk
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=tools \
  python3 -m vibescreen_evidence.device_info \
  --serial <redacted-serial> \
  --no-connect \
  --output <evidence-dir>/device-info.raw.json
```

Observed values:

```text
state: device
manufacturer: nubia
model: P0110
device: pacific
Android release: 16
SDK: 36
```

The generated device-info file was then redacted so both `adb_serial` and
`device_serial` are `nubia-p0110-pacific-device-1`, the local Android SDK path
is replaced with `[redacted-android-sdk]`, and the fingerprint tail is replaced
with `[redacted-build]` markers.

Host identity checks used:

```bash
sysctl -n hw.model
sw_vers
shasum -a 256 /Applications/Vibe\ Screen.app/Contents/MacOS/Vibe\ Screen
codesign -dv --verbose=4 /Applications/Vibe\ Screen.app
```

Camera and formal-package preflight used read-only checks:

```bash
system_profiler SPCameraDataType
find docs/changes/2026-08-04-phase-0-baseline/evidence -maxdepth 2 \( -name raw-camera.mov -o -name manifest.json -o -name samples.csv -o -name latency-evidence-report.json \) -print | sort
```

Transport readiness checks used:

```bash
adb -s <redacted-serial> reverse --list
lsof -nP -iTCP:54321 -sTCP:LISTEN
adb -s <redacted-serial> shell ip addr show wlan0
adb -s <redacted-serial> shell ip route
adb -s <redacted-serial> shell pidof dev.telemachus.display
```

Latency preflight command:

```bash
make evidence-latency-preflight \
  EVIDENCE_DIR=docs/changes/2026-08-04-phase-0-baseline/evidence/2026-08-28-nubia-p0110-latency-current-base-blocked \
  LATENCY_DEVICE_INFO=docs/changes/2026-08-04-phase-0-baseline/evidence/2026-08-28-nubia-p0110-latency-current-base-blocked/device-info.json \
  LATENCY_PREFLIGHT_INPUT=docs/changes/2026-08-04-phase-0-baseline/evidence/2026-08-28-nubia-p0110-latency-current-base-blocked/preflight-input.json \
  LATENCY_REPOSITORY_REVISION=dd6978cb5e8e36b6aa15995361ed28ee54cd6b3e
```

Result: exit `2`, meaning blocked readiness rather than malformed input or a
failed latency measurement. The generated `latency-preflight.json` reports
`status=blocked` and `can_close_performance_gate=false` for all three profiles.

End-of-run process check:

```bash
pgrep -x sfltool || true
```

Result: no `sfltool` process was present at the end of the run.

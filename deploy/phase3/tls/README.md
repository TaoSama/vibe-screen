# TLS files

Production expects `fullchain.pem` and `privkey.pem` in this directory. Both
paths are ignored by Git. Use certificates issued for the relay hostname; do
not use development self-signed material in production.

